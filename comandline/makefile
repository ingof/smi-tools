CC = gcc
OPT = -g -Wall

# default target
default: smi

# compile only smi commandline
smi:  smi.c
	$(CC) $(OPT) -o smi smi.c

# removes all object, temporary an executable files
clean:
	$(RM) count *.o *~
  
